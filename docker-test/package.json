{
  "name": "jasdbx-docker-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "test": "tsx test-backend.ts",
    "test:all": "npm run docker:up && sleep 10 && npm run test"
  },
  "dependencies": {
    "jasdbx": "file:.."
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
